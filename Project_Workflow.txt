-------------------------------------DATA INSERTION IN MONGODB-----------------------------------------
1. Create data insertion folder and data_insertion file.✅
2. Write code to insert data into MONGODB database✅ 

----------------------------------------SETUP CONFIGURATION-------------------------------------------
1. Add block of code on setup.py file that helps to make the meta-data of the project such as Project_name, Version, find_packages().
2. Must Installed -e. which will tell Python to treat your project like a real package — even during development.

-------------------------------------------CONSTANT VARIABLES-------------------------------------------
1. Declare variables within Constants/__init__.py file variables specifically used in each components of the Project workflow.✅

-------------------------------------MONGODB CLIENT IN CONFIGURATION------------------------------------
1. Add code to configuration/mongo_db_connections.py file and define the func for mongodb connection.✅

-----------------------------------FETCH DATA FROM DATABASE USING MDB CONNECTION------------------------
1. Inside "data_access" folder, add code to Fecth_Data func that will use mongo_db_connections.py to connect with DB, and the
   fetch data in key-val format and transform that to df✅ 

----------------------------------------INITIALIZE THE DVC AND GIT---------------------------------------
1. Git init
2. DVC init
----------------------------------------------DATA INGESTION---------------------------------------------
0. Add constants in Constant/__init__.py file for Data Ingestion df✅
1. Add code to entity.config_entity.py file for DataIngestionConfig class✅
2. Add code to entity.artifact_entity.py file for DataIngestionArtifact class✅
3. Add code to components.data_ingestion.py file
4. Add pipeline runner block.
5. Add data ingestion code to training pipeline file
6. Run demo.py (set mongodb connection url first, see next step)

---------------------------------------------DVC & GITHUB------------------------------------------------
1. Run dvc repro 
2. Push code to git 

----------------------------------------------UTILS FUNCTION----------------------------------------------
1. Add all required function in main_utils.py file that will be use in remaining components of the pipeline.

---------------------------------------------CONFIG YAML FILE----------------------------------------------
1. List down all the name of the columns and dataset information in a config.yaml file for the data validation.

----------------------------------------------DATA VALIDATION CONSTANT-----------------------------------------
1. Add the data validation directory name as "data_validation".
2. Add the name of data validation report as "report.yaml"

-----------------------------------------------DATA VALIDATION CONFIG/ARTIFACT---------------------------------
1. Add code data validation configuration entities in config_entity.py file.
2. Add code data validation artifact entities in artifact_entity.py file.

----------------------------------------------DATA VALIDATION--------------------------------------------------
1. Add code on data validation file
2. Add pipeline runner block.

----------------------------------------------TRAINING PIPELINE------------------------------------------------
1. Add data validation code to training pipeline file

--------------------------------------------DATA TRANSFORMATION CONSTANTS--------------------------------------
1. Add constant variables in constant file

------------------------------------------DATA TRANSFORMATION CONFIG/ARTIFACT----------------------------------
1. Add data transformation configuration code in config_entity.py file
2. Add data transformation artifacts code in artifact_entity.py file.

---------------------------------------------------ESTIMATOR-----------------------------------------------------
1. Add codes for TargetFeatureMapping class to map the target feature value like Yes=1, No=0

----------------------------------------------DATA TRANSFORMATION-----------------------------------------------
1. Add code data transformation to transform features
2. Add pipeline runner block.

----------------------------------------------TRAINING PIPELINE------------------------------------------------
1. Add data transformation code to training pipeline file

-------------------------------------------------MODEL YAML----------------------------------------------------
1. Add schemas for the models in model.yaml 

-------------------------------------------MODEL TRAINER CONSTANT-----------------------------------------------
1. Add the model trainer directory name as "model_trainer"
2. Add the trained model directory name as "trained_model"
3. Add the trained model name as "model.pkl"
4. Add the model config file path as "config, model.yaml"

------------------------------------------MODEL TRAINER CONFIG/ARTIFACT-----------------------------------------
1. Add model trainer configuration code in config_entity.py file
2. Add model classification metric and trainer artifacts in artifact_entity.py file.

------------------------------------------------MODEL_REGISTRY---------------------------------------------------
1. Add code for all model names in dict format

---------------------------------------------------ESTIMATOR-----------------------------------------------------
1. Add codes for churnmodel class that helps to create the pipeline of transformation model and trained model

---------------------------------------------------MLFLOW SETUP-------------------------------------------------
1. Import MLFLOW in workflow
2. Run this code in terminal : mlflow ui

---------------------------------------------------MODEL TRAINER-------------------------------------------------
1. Add code on model_trainer.py to extract, train and create artifact
2. Also add mlflow code in model training to track its parameters and model performance.

-----------------------------------------------AWS IAM USER ACCOUNT----------------------------------------------
1. Before moving to next component of Model Evaluation, some AWS services setup is needed:
      * Login to AWS console.
      * Keep region set
      * Go to IAM >> Create new user (name: ChurnPrediction)
      * Attach policy >> select AdministratorAccess >> next >> create user
      * Go to the user >> Security Credentials >> Access Keys >> Create access key
      * Select CLI >> agree to condition >> next >> Create Access Key >> download csv file
      * Set env variables with above csv values using below method:


-----------------------------------------------AWS CREDENTIAL SETUP-----------------------------------------------
1. Add aws access key: AWS_ACCESS_KEY_ID_ENV_KEY=os.getenv('AWS_ACCESS_KEY_ID')
2. Add aws secret access: AWS_SECRET_ACCESS_KEY_ENV_KEY=os.getenv('AWS_SECRET_ACCESS_KEY')
3. Add region name: REGION_NAME=os.getenv('REGION_NAME')

-----------------------------------------------AWS S3 BUCKET CONFIGURATION---------------------------------------
1. Go to S3 service >> Create bucket >> Region: eu-north-1a >> General purpose >>
        Bucket Name: "bucket_name(underscore not allowed for bucket name)" >> uncheck: "Block all public access" 
        and acknowledge >> Hit Create Bucket

-----------------------------------------------AWS S3 BUCKET CONFIGURATION---------------------------------------
1. Now inside "src/Cloud_storage/aws_storage" code needs to be added for the configurations needed to pull 
        and push model from AWS S3 bucket. 
2. Inside "entity" dir we will have an "s3_estimator.py" file containing all the func to pull/push
        data from s3 bucket.

-----------------------------------------------MLFLOW DAGSHUB CONNECTION-----------------------------------------
import dagshub
model_tracking_uri="https://dagshub.com/Saroj94/Churn-MLOps.mlflow"

dagshub.init(repo_owner='Saroj94', repo_name='Churn-MLOps', mlflow=True)

import mlflow
with mlflow.start_run():
  mlflow.log_param('parameter name', 'value')
  mlflow.log_metric('metric name', 1)

----------------------------------------------MODEL EVALUATION CONSTANT----------------------------------------------
1. Ensure below info in constants.__init__.py file:
            MODEL_EVALUATION_CHANGED_THRESHOLD_SCORE: float = "number"
            MODEL_BUCKET_NAME = "bucket_name"
            MODEL_PUSHER_S3_KEY = "model-registry"

------------------------------------------MODEL EVALUATION CONFIG/ARTIFACT--------------------------------------------
1. Add model evaluation config that assigned in constant.py
2. Add model evaluation artifact 

------------------------------------------MODEL PUSHER CONFIG/ARTIFACT--------------------------------------------
1. Add model pusher config that assigned in constant.py
2. Add model pusher artifact 

------------------------------------------DAGSHUB AUTHORIZATION SETUPS--------------------------------------------------
1. Generate the dagshub token : go to Dagshub -----> setting -----> Token ---------> Generate new token
2. Save tokens with secret variable: >>>>>>>>Token<<<<<<<<

----------------------------------------------CI/CD PUSH IN GITHUB-------------------------------------------------
1. Add all dependencies in requirements.txt
2. Add .github/workflows/ci_cd.yaml in the main workflow.
---------------------------------------------DOCKER FILE CREATION-------------------------------------------------
1. containerize all dependencies on docker file











